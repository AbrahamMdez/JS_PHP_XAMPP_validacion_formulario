/* const formDate = document.querySelector('#formDate');
const date1 = document.querySelector('#date1');
const date2 = document.querySelector('#date2');

const totalDays = () => {
    
    let milisegDay = 24 * 60 * 60 * 1000;

    let milisegSpendt = Math.abs(new Date (date1).getTime() - new Date (date2).getTime());

    let daysSpendt = Math.round(milisegSpendt/milisegDay);

    console.log(newFormatDate1);
    console.log(newFormatDate2);
    console.log(milisegDay);
    console.log(milisegSpendt);
    console.log(daysSpendt);
};

document.querySelector('#totalDays').onclick = totalDays();   */

/* formDate.addEventListener('submit', e => {
    e.preventDefault();
});

date1.addEventListener('change', e => {
    const dateFirstDay = e.target.value;
    
});

date2.addEventListener('change', e => {
    const dateSecondDay = e.target.value;
    
}); */